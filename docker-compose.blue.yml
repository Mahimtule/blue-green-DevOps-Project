version: '3'

services:
  blue-app:
    image: myapp:v15
    container_name: blue-app
    environment:
      - VERSION=BLUE
    ports:
      - "3002:3000"
    networks:
      - devops-net

networks:
  devops-net:
    external: true
